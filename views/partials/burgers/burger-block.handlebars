<li class="list-group-item listBurgers text-white bg-dark mb-1">
		{{burger_name}}
	{{#if devoured}}
		<button class="change-devoured btn btn-outline-primary" data-id="{{id}}" data-newdevour="{{devoured}}">
			DEVOUR
		</button>
	{{/if}}
</li>
